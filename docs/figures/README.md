# docs/figures

Images used by `README.md` and the LaTeX report.

## hero.jpg — pending

The README has the hero image commented out so it does not render as a
broken link. Drop the photo here and uncomment the block at the top of
`README.md`.

**For now:** the telescope with the printed parts fitted, shot outdoors in
daylight, no flash. Wide enough to read as a telescope, close enough to
see that the parts are printed.

**Later, and this is the one that matters:** replace it with the
**acclimation curve** — mirror temperature against time, fan off versus
fan on, from the DS18B20 series. That plot is the entire argument of this
repository in one image, and almost nobody publishing telescope
modifications can show it.

Export plots as PNG at 2× the display width so they stay sharp on a
retina screen, or as SVG if there is no photographic content.
